export default defineAppConfig({
  ui: {
    colors: {
      primary: 'blue',
      secondary: 'sky',
      neutral: 'zinc'
    },
    button: {
      slots: {
        base: 'cursor-pointer'
      }
    }
  }
})
